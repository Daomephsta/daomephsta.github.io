<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>{{ page.title }}</title>
        <link rel="stylesheet" href="/stylesheets/blog-post.css">
    </head>
    <body>
    <div>
      <h2 id="title">{{ page.title }}</h2>
      {{ page.content }}
    </div>
  </body>
</html>